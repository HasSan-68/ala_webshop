{% extends 'base.html.twig' %}

{% block title %}Producten1{% endblock %}

{% block body %}
    <h1>Producten1</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producten1.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ producten1.code }}</td>
            </tr>
            <tr>
                <th>Discription</th>
                <td>{{ producten1.discription }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ producten1.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ producten1.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producten1_index') }}">back to list</a>

    <a href="{{ path('producten1_edit', {'id': producten1.id}) }}">edit</a>

    {{ include('producten1/_delete_form.html.twig') }}
{% endblock %}
